This is a paragraph.

This is the :guilabel:`Sales` tab.

This is a paragraph.

When no route is selected, Odoo selects the :guilabel:`Route` specified in the *Inventory* tab of
the product form.

This is a paragraph.

Configure the discount in the :guilabel:`Pricing` tab of the product form.

This is a paragraph.

Click the :guilabel:`Settings` button, not a tab.

This is a paragraph.

This mentions a :guilabel:`Setup` tablet, which is not a tab.
